//! BusBar workspace root package.
//!
//! Hosts the Cucumber acceptance harness (`features/steps.rs` running the
//! co-located Gherkin files). The toolchain itself lives in `crates/*` —
//! this crate intentionally contains no implementation.
