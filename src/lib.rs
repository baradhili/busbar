//! BusBar workspace root package.
//!
//! Hosts the Cucumber acceptance harness (`tests/cucumber.rs` running
//! `features/`). The toolchain itself lives in `crates/*` — this crate
//! intentionally contains no implementation.
