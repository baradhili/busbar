# Tools

Home for the spec-example extractor (pulls fenced `esld` blocks from
`Design/esld-spec.md` for `features/spec-examples.feature`) and the SVG
snapshot normalizer. Both land with their milestones — see
`Design/esld-implementation.md` §6 and §12.
