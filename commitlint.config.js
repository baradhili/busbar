// Conventional Commits, per AGENTS.md and existing history (feat/fix/docs/
// test/build/chore(deps)/refactor…). Scopes stay free-form (layout, deps,
// …); rules are the shared preset, unchanged.
module.exports = {
  extends: ['@commitlint/config-conventional'],
};
