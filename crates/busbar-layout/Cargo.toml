[package]
name = "busbar-layout"
description = "Deterministic layered layout solver (spec §16.1)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
busbar-ir = { path = "../busbar-ir" }
busbar-syntax = { path = "../busbar-syntax" }

[dev-dependencies]
busbar-syntax = { path = "../busbar-syntax" }

[lints]
workspace = true
